<div class="container">
  <%= render "form.html", Map.put(assigns, :action, Routes.post_path(@conn, :create)) %>
</div>
<div class="container">
  <%= for post <- @posts do %>
    <%= render "show.html", %{post: post} %>
  <% end %>
</div>
